# WC4 地形编辑器（Android）项目交接说明

生成日期：2026-08-10　（本文档用于更换大模型后快速接手，新模型请先通读本文档再改代码）

## 1. 这是什么项目

- **WC4 地形/地图编辑器**：安卓手机 App（纯 Java + 原生 View，无第三方 UI 框架），用于读取、编辑、保存《世界征服者4》的地图文件。
- 能编辑的文件：`.btl`（战役/征服关卡文件）、`.bin`（世界底图 world.bin / mapN.bin）。
- 核心用途：涂地形、放建筑/城市/兵种、编辑军团/国家、做省区规划、扩展地图，导出后放回游戏使用。
- GitHub 仓库：`github.com/luyuanmarker/Terrain-Editor-Android`（分支 main）。
- 工作目录：`/Users/mrjoe_chen/Desktop/综合开发/wc4地形编辑器移动端的开发/地形编辑器测试版`。

## 2. 技术栈与构建

- Android Java；compileSdk 34 / minSdk 26 / targetSdk 34；AGP 8.7.0。
- 界面全部代码动态创建，无布局 XML（只有一个 AndroidManifest）。
- 构建命令：`./gradlew :app:assembleDebug`
- APK 输出：`app/build/outputs/apk/debug/app-debug.apk`（通常再复制到桌面 `地形编辑器测试版.apk`）。
- 推 GitHub 偶尔超时：`git -c http.version=HTTP/1.1 push origin main`。

## 3. 目录与核心类

| 文件 | 职责 |
|---|---|
| `app/src/main/java/com/xckeji/bj/MainActivity.java` | 全部 UI 与交互（约 4000 行）：首页、顶部工具栏、左侧面板、右侧面板、各类对话框/悬浮窗、保存 |
| `file/FileParser.java` | BTL/BIN 解析与写入核心：loadBTL / loadBIN / loadConquestTerrain / saveAsBTL / saveAsBIN / cropMap / expandMapGeneric / extendConquest / patchProvince 等 |
| `model/MapData.java` | 内存地图模型：tiles、buildingIds、provinces（省规划）、belongs（军团归属）、legions、armies、buildings、coordBase、官方海岸线查表 |
| `model/TerrainTile.java` | 16 字节地块 |
| `render/HexMapView.java` | 地图渲染与手势（平移/缩放/笔刷/省区视图/编号文字） |
| `model/ArmyConfig.java` / `CountryData.java` | 兵种基础数据 / 国家 ID→名称表 |
| `assets/` | 模板（stage10103.btl）、兵种 json、贴图、国旗等 |

## 4. 文件格式（务必理解）

- **BTL**：128 字节头（0x04=mapId，0x08/0x0C=captureX/Y，0x10/0x14=宽/高，0x18=军团数，0x1C=建筑数，0x20=兵种数，0x58=总格数）+ 军团段（300B/条，国家ID在 0x4，地块颜色在 0x28）+ 地形段（16B/格，**只有 mapId==0 的战役 BTL 才有**）+ 省规划（2B/格，值=省代表格坐标）+ 军团归属（1B/格）+ 建筑段（32B/条）+ 兵种段（v1=48B / v3=64B）+ 事件/援军等尾段。
- **征服 BTL**：`mapId != 0` → 无内嵌地形，地形来自世界底图（world.bin / mapN.bin），BTL 用 captureX/Y + 宽高从底图"截取"。
- **BIN（world.bin 官方格式，规则 rule_wc4_map.xml）**：16 字节头（mapTag/mapVersion/mapW/mapH）+ 宽×高×16 地形 + 宽×高×2 省规划。
- **坐标约定**：官方整合版征服文件把省规划/建筑/兵种坐标存成"世界坐标" = 本地坐标 + coordBase（captureY×宽+captureX），编辑器自动检测（`coordBase`）。
- **权威参考**：桌面 `综合开发/java编辑器/rule/` 下的规则 XML（rule_wc4_btl.xml、rule_wc4_map.xml、rule_wc4_mapBin.xml），以及反编译的官方编辑器源码 `综合开发/java编辑器/src`（com.zhfy.game，官方海岸线/省区算法都在里面）。

## 5. 已实现的主要功能

- **首页**：紫色渐变 + 4 个按钮（打开战役 stage / 打开征服 / 新建战役 / 打开地图）；先选文件、读取成功后才进入编辑器，取消则留在首页。
- **编辑器界面**：顶部工具栏（新建BTL / BTL数据 / 地图 / 视图）；左侧面板（保存 + 显示省区规划开关；战役/征服模式下隐藏"打开"和声音按钮）；右侧面板为 **ImGui 风格分节折叠**（笔刷 / 地形 / 设施 / 兵种 / 城市 / 省区）。
- **地形**：涂色（**官方海岸线算法**：海面波浪 id = 6 邻居海陆掩码查表 + 10）、随机地形、扩展地图（战役用 expandMapGeneric；征服用官方模式扩展 world.bin，保存只输出官方格式 world.bin）、截取地图。
- **建筑/城市**：设施列表放置；**新放城市的地块自动成为新省区代表格**；城市 32 字段编辑。
- **兵种**：兵种图标添加（点图标锁定 → 点地图放置，归属跟随地块）；**兵种属性 48 字段编辑在独立 ImGui 悬浮窗**（标题栏可拖动）。
- **军团/国家**：军团列表（旗+颜色+文字）、军团 300 字段编辑；「新建国家」（输入国家 ID+名称，写入 CountryData，在军团详情把"国家"字段设为该 ID 即可使用）。
- **省区规划（官方语义）**：省规划值 = 省代表格坐标，值相同 = 同一省。「省区编辑」开关 + 「取省/刷省」按钮（对应官方 C/V）+ 省区列表；**省区视图在地图上显示每省编号文字**（#省区值）。
- **征服**：自动匹配世界底图（world.bin/mapN.bin 优先级），可像战役一样编辑，保存输出 BTL + 世界 BIN。
- **信息**：左上角显示 FPS / 设备 / 版本号 / 当前时间。

## 6. 近期改动日志（供参考）

- 首页四按钮 + 先读文件后进入编辑器
- 右侧面板 ImGui 化：深色扁平、分节折叠、兵种图标入分节、兵种属性独立悬浮窗
- 省区规划：官方取省/刷省、省区编号文字显示、新城市自动成省区
- 官方海岸线算法（波浪 id 查表 + 10，96% 复现原图）
- 征服扩展：官方模式只扩展 world.bin（官方格式：头+地形+省规划）
- 崩溃日志：写 `Download/crash_log.txt`

## 7. 已知问题 / 注意事项

- UI 改动验证需要**卸载重装** APK（覆盖安装有时不生效）。
- 自定义国家没有国旗图标（国旗资源只到 48 号）。
- 大图性能已做贴图缓存优化；若继续卡顿可从 HexMapView 入手。
- 桌面 `综合开发/java编辑器/MapEditAndroid` 是官方编辑器移植版（另一个 App，勿混淆）。

## 8. 给接手模型的工作提示

1. 改代码前先读 `MainActivity.java`（UI）、`FileParser.java`（格式）、`MapData.java`（模型）、`HexMapView.java`（渲染）。
2. 保存逻辑集中在 `MainActivity.doSave()`：官方模式（conquestExtended）只输出 world.bin，其余模式输出 BTL+世界 BIN。
3. 编译用 `./gradlew :app:assembleDebug`；若沙箱报锁文件错误，用非沙箱（escalated）执行。
4. 验证 BTL/BIN 逻辑可用桌面素材：`综合开发/征服/conquest1.btl` + `world2.bin`。
5. 界面文字均为中文；用户偏好简洁直给的沟通。
