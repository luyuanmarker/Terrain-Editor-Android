# WC4 地形编辑器（Android）项目交接说明

生成日期：2026-08-29　（本文档用于更换大模型后快速接手，新模型请先通读本文档再改代码）

## 1. 这是什么项目

- **WC4 地形/地图编辑器**：安卓手机 App（纯 Java + 原生 View，无第三方 UI 框架），用于读取、编辑、保存《世界征服者4》的地图文件。
- 能编辑的文件：`.btl`（战役/征服关卡文件）、`.bin`（世界底图 world.bin / mapN.bin）。
- 核心用途：涂地形、放建筑/城市/兵种、编辑军团/国家、做省区规划、扩展地图，导出后放回游戏使用。
- GitHub 仓库：`github.com/luyuanmarker/Terrain-Editor-Android`（分支 main）。
- 工作目录：`/Users/mrjoe_chen/Desktop/综合开发/wc4地形编辑器移动端的开发/地形编辑器测试版`。
- 当前 HEAD：`5712793`（基础版 + 精英兵种 + 设施/编制/国旗显示 + 素材）。

## 2. 技术栈与构建

- Android Java；compileSdk 34 / minSdk 26 / targetSdk 34；AGP 8.7.0。
- 界面全部代码动态创建，无布局 XML（只有一个 AndroidManifest）。
- 构建命令：`./gradlew :app:assembleDebug`
- APK 输出：`app/build/outputs/apk/debug/app-debug.apk`（通常再复制到桌面 `Terrain Editor正式版.apk`）。
- 推 GitHub 偶尔超时：`git -c http.version=HTTP/1.1 push origin main`。
- 子工程 `WC4SaveManager/`：独立的「WC4 存档管理器」App（开放存档目录工具，纯 Java，无依赖），构建方式相同。

## 3. 目录与核心类

| 文件 | 职责 |
|---|---|
| `app/src/main/java/com/xckeji/bj/MainActivity.java` | 全部 UI 与交互（约 7000 行）：首页、顶部工具栏、左侧面板、右侧面板、数据面板、各类对话框/悬浮窗、保存 |
| `file/FileParser.java` | BTL/BIN 解析与写入核心：loadBTL / loadBIN / loadConquestTerrain / saveAsBTL / saveAsBIN / cropMap / expandMapGeneric / extendConquest / patchProvince 等 |
| `model/MapData.java` | 内存地图模型：tiles、buildingIds、provinces（省规划）、belongs（军团归属）、legions、armies、buildings、traps、coordBase、官方海岸线查表 |
| `model/TerrainTile.java` | 16 字节地块 |
| `render/HexMapView.java` | 地图渲染与手势（平移/缩放/笔刷/省区视图/编号文字/兵种/地雷/设施图标） |
| `model/ArmyConfig.java` / `CountryData.java` | 兵种基础数据（含 elite 精英标记）/ 国家 ID→名称表 |
| `assets/` | 模板（stage10103.btl）、兵种 json、贴图、国旗、`legion/` 兵种图标（1~128 + `_r_`）、`image/status/` 设施/编制图标、`json/city_names.json`（3966 条城市名素材） |

## 4. 文件格式（务必理解）

- **BTL**：128 字节头（0x04=mapId，0x08/0x0C=captureX/Y，0x10/0x14=宽/高，0x18=军团数，0x1C=建筑数，0x20=兵种数，0x58=总格数）+ 军团段（300B/条，国家ID在 0x4，地块颜色在 0x28）+ 地形段（16B/格，**只有 mapId==0 的战役 BTL 才有**）+ 省规划（2B/格，值=省代表格坐标）+ 军团归属（1B/格）+ 建筑段（32B/条）+ 兵种段（v1=48B / v3=64B）+ 事件/援军等尾段。
- **征服 BTL**：`mapId != 0` → 无内嵌地形，地形来自世界底图（world.bin / mapN.bin），BTL 用 captureX/Y + 宽高从底图"截取"。
- **BIN（world.bin 官方格式，规则 rule_wc4_map.xml）**：16 字节头（mapTag/mapVersion/mapW/mapH）+ 宽×高×16 地形 + 宽×高×2 省规划。
- **坐标约定**：官方整合版征服文件把省规划/建筑/兵种坐标存成"世界坐标" = 本地坐标 + coordBase（captureY×宽+captureX），编辑器自动检测（`coordBase`）。
- **权威参考**：桌面 `综合开发/java编辑器/rule/` 下的规则 XML（rule_wc4_btl.xml、rule_wc4_map.xml、rule_wc4_mapBin.xml），以及反编译的官方编辑器源码 `综合开发/java编辑器/src`（com.zhfy.game，官方海岸线/省区算法都在里面）；枭雄 PyQt 编辑器在 `综合开发/枭雄/`（map_extension.py 的渲染/图标布局是仿照对象）。

## 5. 已实现的主要功能

- **首页**：紫色渐变 + 5 个按钮（打开战役 stage / 打开征服 / 新建战役 / 打开地图 / 打开 APK）+ 左侧「读取游戏文件夹」入口（城市名素材管理）。
- **编辑器界面**：顶部工具栏（撤销 / 新建BTL / 编辑器下拉：btl数据、数据面板、属性面板开关 / 地图下拉：扩展、截取、随机地形、随机兵力、城市列表 / 视图下拉：军团列表、省规划视图、工具面板、导入底图、导入图填、遮罩开关）；左侧面板（打开/保存 + 导出测试BTL和底图 + 显示省区规划开关 + 音乐/音效）；右侧 ImGui 风格属性面板（可缩放/拖动/折叠）；数据面板右侧弹出 1/3 屏（军队/城市/省区/地雷四页）。
- **地形**：涂色（官方海岸线算法：海面波浪 id = 6 邻居海陆掩码查表 + 10）、随机地形、扩展地图（战役 expandMapGeneric；征服用官方模式扩展 world.bin，保存只输出官方格式 world.bin）、截取地图（起点→终点框选→保存）、随机兵力（按比例 100=满屏/70=七成）。
- **建筑/城市**：设施列表放置；新放城市的地块自动成为新省区代表格；城市 32 字段编辑；城市归属可选国家（带国旗）；城市名可选（内置 city_names.json）。
- **兵种**：兵种图标添加（点图标锁定 → 点地图放置，归属跟随地块）；兵种属性 48 字段编辑在数据面板「军队」页；**精英兵种**：1~40 号 + `elite>0` 的 41+ 号（T-44 等）可显示/添加，图标加载 1~128 号并支持 `_r_` 变体兜底。
- **军团/国家**：军团列表（旗+颜色+文字）、军团 300 字段编辑；「新建国家」（输入国家 ID+名称）；军团详情可改国家归属。
- **省区规划（官方语义）**：省规划值 = 省代表格坐标，值相同 = 同一省。「省区编辑」开关 + 「取省/刷省/撤销上一步」（对应官方 C/V）；省区视图在地图上显示每省编号文字；新建城市自动成为新省区。
- **地雷**：官方图标（陆/海）+ 等级角标 + 右下角国旗；可编辑等级/军团/血量/删除。
- **显示（仿枭雄）**：兵种右下角国旗 + 编制黑底块（formation.png 重复 1~4）；建筑左上角小国旗 + 建筑上方设施图标网格（机场/补给/工厂/科研/导弹/核弹 + lv 等级角标）+ 右侧防空（1x/2x/3x）/雷达图标；地雷右下角国旗。
- **征服**：自动匹配世界底图（world.bin/mapN.bin 优先级），可像战役一样编辑，保存输出 BTL + 世界 BIN。
- **信息**：左上角 FPS / 设备 / 版本 / 日期 / 时间 / 当前文件；左下角交流群文字。
- **网络后台**：远程公告、设备黑名单拦截（断网也拦，本地缓存）、版本停用（versions.txt）、崩溃上报、设备使用统计（宝塔 server/ 目录，用户端代码已更新，服务器端需同步最新 index.php/collect.php）。

## 6. 近期改动日志（供参考）

- 2026-08-29 提交 `5712793`：回退到 `9d3fe83` 基础版本，**只保留**精英兵种功能与素材（legion 42~101 + `_r_`、image/status、city_names.json）；随后恢复设施/编制/国旗显示（见第 5 节）。
- 之前未提交又被回退的改动（素材保留、代码已移除）：地图显示城市中文名、游戏文件夹管理 UI、枭雄式地形渲染（跨格贴图）、去掉采样色马赛克、大地图后台整图缓存。
- 更早（9d3fe83）：地雷/省区笔刷/兵种图标选择/城市编辑增强/触控优化/官方扩展/导出测试BTL/远程公告/拦截/版本停用/崩溃上报。
- 官方海岸线算法（波浪 id 查表 + 10）；征服扩展只输出 world.bin；崩溃日志写 `Download/crash_log.txt`。

## 7. 已知问题 / 注意事项

- UI 改动验证需要**卸载重装** APK（覆盖安装有时不生效）。
- 自定义国家没有国旗图标（国旗资源只到 48 号）。
- 大图性能：整图缓存仍在主线程（后台缓存优化被回退），大地图打开/缩放可能卡，需再优化。
- Android 11+ 无法直接读其他 App 私有目录：征服底图/存档需通过公开目录或 root/备份方式。
- 桌面 `综合开发/java编辑器/MapEditAndroid` 是官方编辑器移植版（另一个 App，勿混淆）。
- 回退前的完整代码备份在 `/tmp/wc4_backup_20260829/`（MainActivity/ArmyConfig/HexMapView），需要找回被回退功能时可从这里摘取。

## 8. 给接手模型的工作提示

1. 改代码前先读 `MainActivity.java`（UI）、`FileParser.java`（格式）、`MapData.java`（模型）、`HexMapView.java`（渲染）。
2. 保存逻辑集中在 `MainActivity.doSave()`：官方模式（conquestExtended）只输出 world.bin，其余模式输出 BTL+世界 BIN。
3. 编译用 `./gradlew :app:assembleDebug`；若沙箱报锁文件错误，用非沙箱（escalated）执行。
4. 验证 BTL/BIN 逻辑可用桌面素材：`综合开发/征服/conquest1.btl` + `world2.bin`。
5. 界面文字均为中文；用户偏好简洁直给的沟通，讨厌 AI 味和啰嗦。
6. 用户近期方向：仿枭雄/官方的显示细节（设施、编制、国旗）、存档管理工具、性能优化；改动前如涉及方向选择，先复述确认。
