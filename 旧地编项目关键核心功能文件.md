# 旧地编（官方 MapEdit 反编译源码）项目关键核心功能文件

> 路径：`综合开发/旧编辑器源码/`（`src/` 为桌面反编译源码，`MapEditAndroid/` 为安卓版工程）
> 技术栈：Java + libGDX（渲染/输入），桌面版带 Swing 弹窗，数据格式由 rule XML 定义。

## 一、核心功能 → 文件对照表

| 核心功能 | 关键文件 | 说明 |
|---|---|---|
| BTL 读写 / 模型 | `model/content/WC4BTLDAO.java` | bm0~bm17 各段解析、保存、更新（updateBtlData、updateHexagon、initTestConquest、initMapBin） |
| 世界底图 BIN 模型 | `model/content/Wc4MapBinDAO.java` | 地块列表、省规划 regionList、索引映射；扩展/收缩/海岸线/省区重算 |
| 剧本/征服内容模型 | `model/content/Wc4StageDAO.java` | 城市/兵种等 stage、归属 bbBelong、省区关联、随机地雷、updateHexagon |
| 世界底图编辑器画面 | `screen/actor/Wc4BgActor.java` | 底图渲染、地形/装饰/高速路编辑、扩展、保存bin、导出测试btl和底图 |
| 剧本编辑器画面 | `screen/actor/Wc4StageActor.java` | 渲染（地形/省区/建筑/兵种/地雷）、键盘操作、扩展联动、保存 |
| 地图编辑器界面 | `screen/Wc4MapScreen.java` | 按钮加载与分派（保存bin / 导出测试btl和底图 / 加载 / 返回） |
| 剧本编辑器界面 | `screen/Wc4StageScreen.java` | 按钮分派、剧本切换 |
| 保存/加载总调度 | `framework/GameFramework.java` | saveWc4MapDaoByMapId、saveWc4MapBinByMapId、saveWc4StageDaoByStageId、按 id 读取 |
| 二进制序列化 | `framework/tool/BTLTooL.java` | 按 rule XML 把对象写/读成 bin/btl 字节 |
| 像素换算 / 地图坐标 | `framework/GameMap.java` | 格子索引 ↔ 屏幕像素（112×128 格子、0.32 缩放） |
| 公用工具 | `framework/ComUtil.java`、`framework/GameUtil.java` | 随机、文本、UI 工具 |
| 建筑/兵种定义 | `model/content/DefDAO.java`、`config/ResConfig.java` | 从 def_*.xml 读建筑、兵种、国家、地形配置 |
| 坐标模型 | `model/framework/Coord.java` | 格子坐标与索引 |
| 格式定义 | `rule/rule_wc4_btl.xml`、`rule/rule_wc4_map.xml`、`rule/rule_wc4_mapBin.xml` | btl / 底图 / 测试底图的字节结构 |
| 按钮布局 | `MapEditAndroid/app/src/main/assets/config/config_layout.xml` | 各界面按钮位置与 functionId |

## 二、重点文件详细说明

### 1. WC4BTLDAO.java —— BTL 总模型

- `parseBtl / LoadBtl`：按 rule_wc4_btl.xml 读全部段（bm0 头部、bm1 军团、bm2 地形、bm3 省规划、bm4 归属、bm5 建筑、bm6 兵种、bm7 陷阱、bm8 方案、bm9 天气、bm10 事件、bm11~bm15 援军/空袭/放置/首都、bm16/17 战略/空中支援）。
- `updateBtlData(mapbin, stage, ifWorld)`：保存前重建——世界模式 bm3 由底图生成；独立模式 mapId=0 且地形/省区写入 btl。
- `updateHexagon(hexagonMap)`：扩展后把 bm11~bm15 的坐标按映射平移，旧地图外的删除。
- `initTestConquest(mapbin, w, h, mapId)`：生成测试征服（窗口 宽×(高-4)、capture(0,2)、2 军团）。
- `initMapBin(mapBinW, mapBinH, sumGrid)`：生成测试底图（65534/65535 占位）。

### 2. Wc4MapBinDAO.java —— 世界底图核心

- `mapBinList`：地块列表；`regionList`：省规划（每格一个省代表索引）。
- `extenMapForUp/Down/Left/Right`：四向扩展（顶部/左侧插入，底部/右侧追加，省规划同步插 65535）。
- `retractMapFor*`：四向收缩。
- `checkCoast()`：整图海岸线重算。
- `updateHexagonMap()` / `updateRegionByHexagonMap()` / `resetHexagonMap()`：旧索引→新索引映射，并把省规划值平移。
- `updateRegionForStage(beginX, bm3)`：征服模式把 btl 省规划按截取偏移合并到底图。
- `createStageMapForBm2()` / `createStageRegionForBm3()`：独立地图保存时生成地形/省区。

### 3. Wc4StageDAO.java —— 剧本/征服内容

- `wc4Stages`：城市/兵种等全部内容对象；`hexagons`：索引→stage 映射。
- `getIndex(regionId)`：省代表格 → stage。
- `bbBelong`：归属（由 btl bm4 按 hexagon-beginHexagon 读取）。
- `updateHexagon(hexagonMap)`：扩展后内容坐标平移。
- `autoLandTrap / autoSeaTrap`：按概率随机生成陆/海地雷。

### 4. Wc4BgActor.java —— 世界底图编辑器画面

- 渲染整张底图；`keyInput`：地形组/装饰/高速路编辑。
- `saveMapBin()`：保存 bin（左下按钮1）。
- `saveWc4BgMapBin()`：导出测试 btl 和底图（左下按钮2），并弹两段提示。
- `writePNG()`：导出截图。

### 5. Wc4StageActor.java —— 剧本编辑器画面

- 渲染：地形、省区颜色（归属）、建筑、兵种、地雷图标（sea_trap / land_trap）。
- 键盘操作：C=取省、V=刷省、扩展（51/47/29/32）、随机地雷、随机内容等。
- 扩展后：`stageDAO.updateHexagon` + `btlDAO.updateHexagon` + `ifWorld=false`。

### 6. Wc4MapScreen.java / Wc4StageScreen.java —— 界面与按钮

- 从 `config_layout.xml` 读按钮，按 `functionId` 分派动作：
  - 地图编辑器：2=保存bin、3=导出测试btl和底图、1=加载、0/5=返回
  - 剧本编辑器：9=地图、10=剧本等

### 7. BTLTooL.java —— 序列化引擎

根据 rule XML 的字段定义（偏移/长度/类型）把 DAO 对象读写为字节，是所有文件读写的底层。

### 8. rule XML —— 格式真源

- `rule_wc4_btl.xml`：btl 全部段结构（含 bm3 省规划、bm4 归属、bm7 陷阱等）。
- `rule_wc4_map.xml`：world.bin 底图结构。
- `rule_wc4_mapBin.xml`：测试 `_Bin.bin` 结构（8 字节头 + 每格 2×u16）。

## 三、对我们编辑器的参考价值

- 扩展规则：只改 bin（插行 + 省区 65535 + 省区值平移 + 海岸线），剧本编辑器再联动 btl 坐标。
- 坐标体系：格子索引 = 线性编号；像素换算见 GameMap（112×128、0.32 缩放）。
- 保存约定：地图编辑器输出 bin；剧本编辑器输出自包含 btl（mapId=0）。

