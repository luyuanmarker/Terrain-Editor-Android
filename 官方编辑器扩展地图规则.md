# 官方编辑器（MapEdit）向上扩展地图规则

> 依据：反编译官方编辑器源码（`旧编辑器源码/`）核对整理。
> 涉及文件：`Wc4MapBinDAO`、`Wc4BgActor`、`Wc4StageActor`、`Wc4StageDAO`、`WC4BTLDAO`、`Wc4MapScreen`、`GameFramework`、`config_layout.xml`、`rule_wc4_btl.xml`。

## 一、官方编辑器有两个入口

| 入口 | 说明 | 扩展时改什么 |
|---|---|---|
| 世界底图编辑器（Wc4BgActor / Wc4MapScreen） | 只编辑 world.bin | 只改 bin |
| 剧本/征服编辑器（Wc4StageActor / Wc4StageScreen） | bin + btl 都有 | bin 和 btl 一起改 |

## 二、向上扩展（extenMapForUp）完整流程

输入扩展数量 n、选择填充类型（陆地/海洋）后：

1. `resetHexagonMap()`：给每个地块记录"旧索引"（sourceHexagon = 原线性索引）。
2. 在 bin 地块列表**最前面插入 n×宽 个新地块**（向上扩展 = 顶部插入，旧内容整体下移 n 行），
   同一位置往省规划段（regionList）插入 65535（无省区）。
3. 地图高度 +n。
4. `checkCoast()`：整图重算海岸线。
5. 建立"旧索引→新索引"映射表（向上 = 旧索引 + n×宽），然后把**所有省规划值按该映射平移**（`updateRegionByHexagonMap`）。

填充地块的二进制内容（`getOneMapBin`）：

| 选择 | 地形组/编号 | 装饰组/编号 | 水/路边缘 |
|---|---|---|---|
| 陆地（是） | 组 0 / 编号 255 | 63 / 255 | 0 |
| 海洋（否） | 组 1 / 编号 0 | 63 / 255 | 0 |

### 剧本/征服编辑器额外做的事

在完成上面全部步骤后，还会：

1. `stageDAO.updateHexagon(hexagonMap)`：所有内容对象（城市、兵种等 stage）坐标按同一映射平移；旧地图里不存在的记录删除。
2. `btlDAO.updateHexagon(hexagonMap)`：bm11~bm15（援军、空袭、放置位、首都等含坐标段）同样平移。
3. `initMapWH()` + `ifWorld = false`：编辑器把这张图切成"独立地图"模式。

## 三、保存时（剧本/征服编辑器）

`updateBtlData(mapbin, stage, ifWorld)`：

- 世界模式（ifWorld = true，扩展前）：btl 不写地形（bm2 清空），省区 bm3 由 bin 的 region 重新生成。
- 独立模式（ifWorld = false，扩展后）：`mapId` 改为 0，地形（bm2）、省区（bm3）全部写进 btl；
  输出**一个自包含的 .btl**，不再依赖 bin。

## 四、地图编辑器左下角两个按钮

（config_layout.xml：保存bin = functionId 2，导出测试btl和底图 = functionId 3）

### 1. 保存bin → `saveMapBin()`

把当前（扩展后的）底图直接保存为：

`bin/{地图名}.bin`（例如 world.bin）

结果：**1 个 bin 文件**。

### 2. 导出测试btl和底图 → `saveWc4BgMapBin()`

生成一套测试征服文件：

- `bin/{地图名}_Bin.bin`：按公式生成的新尺寸底图
  （mapBinW = 2×defmapW，mapBinH ≈ 2.068×defMapH，按 125 像素换算格子数）
- `bin/{地图名}_TestConquest.btl`：引用当前底图 w×h 生成的测试征服 btl

并提示：把 `xx_Bin.bin` 改名为 `map?_hd.bin`，并修改 `def_map.xml` 中的 `tile`。

结果：**2 个文件（测试底图 bin + 测试征服 btl）**。

## 五、关键结论（对本编辑器的启示）

1. 官方扩展**不改 btl 头部的截取偏移（captureX/captureY），也不改 bm0 宽高**；
   它改的是 **btl 内容坐标**（让内容跟着地形走）。
2. 官方对 bin 的省规划值做平移；对 btl 的 bm3（省规划）、bm4（归属）这两个"位置数组"本身不动。
3. 剧本编辑器扩展后会把地图切成独立模式（mapId=0），最终产出**自包含 btl**。
4. 之前的"扩展时同步 captureY/captureX"是错误判断，与官方行为不符，应移除。

